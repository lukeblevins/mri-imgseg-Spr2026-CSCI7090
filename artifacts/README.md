# Generated Artifacts

This directory stores generated outputs and is ignored by git except for this file.

Realtime workflows write to `artifacts/realtime/` by default. Override that location with `EEG_OUTPUT_ROOT` when needed.
